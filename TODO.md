# TODO.md

1. The **Token** class has AsDouble() and AsInteger() functions. These might
   be useful in general (or absl equivalents may already exist.) I realized
   this after implementing them. Research whether there are suitable absl
   replacements, and if not, consider putting them in a library.
